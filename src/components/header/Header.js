import './Header.scss'

const Header = () => {
    return (
        <header>
            <img className="zoom-in" src="/pictures/contactBook.jpg" alt="zoom-in" />
        </header>
    );
}

export default Header;